- name: "copy files"
  copy:
  args:
    src: "data/{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "{{ item.mode }}"
    owner: "{{ item.user }}"
    group: "{{ item.group }}"
  with_items: files
  tags: data
